apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    service: usersvc
  name: usersvc
spec:
  type: LoadBalancer
  ports:
  - name: usersvc
    port: 5000
  selector:
    service: usersvc

